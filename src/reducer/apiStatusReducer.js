const initialState={
    isFetching:false,
    error:"",
    paperName:""
}

const apiStatusReducer = (state = initialState, action) => {
     return state
    
  };
  
  export default apiStatusReducer;